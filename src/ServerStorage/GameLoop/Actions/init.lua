local Actions = {}

Actions.sortPlayers = require(script.SortPlayers)
Actions.getEligiblePlayers = require(script.GetEligiblePlayers)
Actions.newPhaseTimer = require(script.NewPhaseTimer)

return Actions